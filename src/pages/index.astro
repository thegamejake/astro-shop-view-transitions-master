---
import Layout from "../layouts/Layout.astro";
import { products } from "../data";
import ProductCard from "../components/ProductCard.astro";
import { previousHref, previousText, tutorialHref, tutorialText, githubText, githubHref } from "../config";
---

<Layout title="Shop">
  <div>
    <h1 class="text-5xl font-bold mb-4">我的商店</h1>
    <h3 class="text-xl text-gray-500 mb-8">
      An example shop using View Transitions with <a class="hover:underline text-black" href="https://astro.build/">Astro</a>.
    </h3>
    <nav class="pb-10">
      <a href={previousHref} target="_blank" rel="noopener noreferrer" class="text-gray-400 hover:underline hover:text-gray-950 py-8">{previousText}</a>
      <span class="text-gray-400 mx-2">&#183;</span>
      <a href={tutorialHref} target="_blank" rel="noopener noreferrer" class="text-gray-400 hover:underline hover:text-gray-950">{tutorialText}</a>
      <a href={githubHref} target="_blank" rel="noopener noreferrer" class="text-gray-400 hover:underline hover:text-gray-950">{githubText}</a>
    </nav>
  </div>
  
  <div class="flex flex-wrap gap-4 py-8">
    {products.map((product) => <ProductCard {product} />)}
  </div>
</Layout>